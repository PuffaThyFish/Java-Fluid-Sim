# Java-Fluid-Sim
A fun side project to improve my Java and OOP project skills.
Each particle follows a simple ruleset that determines how it will move based on surrounding tiles.
Simulation processing methodology can cause drastically different results for pixel sims, but current it uses a bottom-up, only one move per tick system.
I hope to later experiment with other simulation methods, such as random column picking.
